# PLAN：〈任务标题〉

> Fill this template in your own language — the Chinese placeholders are examples, not requirements.（各填写段用你自己的语言即可，中文占位只是示例。）
>
> 填写说明：本文件是引擎B（Goal 循环）的执行输入。每条验收项必须「可验证」——存在客观方法判断完成与否（文件存在/内容匹配/命令退出码/数量达标）。含糊的验收项会被 agent 自由发挥。
> ⛔ 高危命令禁令：本计划涉及的命令**禁止**包含 `rm -rf` / `sudo` / `curl 下载` / `wget` / `git push` / `git reset --hard` / `git clean`（headless 下被拒执行，任务会卡死）。清理类需求改用安全替代并写明。
> 派发前：由派发方**数出实际验收项数并替换 goal 命令中的 N**，禁止直接下发占位符。

- 计划版本：v1
- 创建日期：〈日期〉
- 目标产物目录：〈绝对路径〉
- 进度文件：〈绝对路径〉/PROGRESS.md
- 预计验收项总数：N 项（派发时替换为实际数）

## 背景（1-3 句，agent 无记忆，必须自包含）

〈这个任务为什么做、上下游是什么、有什么前置约定〉

## 验收项清单

1. 〈验收项描述：做什么 + 产出什么文件 + 可验证的完成标准〉
2. ...

（混合型任务的并行项在此单独列出，派发时由桌面端先走引擎A 完成、产物路径写回本节，再派引擎B —— 不要在 goal 循环中途期待 workflow 触发。）

## 依赖顺序

〈哪些项必须先完成、哪些可并行；无依赖写「顺序执行」〉

## 失败与回滚处置

〈某项失败时：跳过继续 / 中止全任务 / 如何回滚已做改动；产物被覆盖的恢复方式〉

## 阶段收口约定（每完成 5 项执行一次）

1. 把本阶段完整产物清单、关键发现、当前状态写入 `<产物目录>/STAGE-{序号}.md`（无损归档，磁盘为准）
2. 在对话中输出一段状态自述：已完成编号 / 关键结论 / 下一阶段计划（这段是给 goal 评估器与未来上下文压缩准备的证据，必须留在对话里）
3. 需要旧阶段细节时从 STAGE 文件按需读回；**只回注索引/摘要，禁止整段塞回**（保前缀缓存命中）

## 约束

- 〈技术约束：语言/框架/目录边界/禁止事项〉
- 〈高危命令禁令：见文件头〉

## 完成标志

全部 N 项验收满足；进度文件每项均有 done 记录且产物 sha256 前 8 位一致；编号零空号；最后一轮已把 `cat 进度文件` 与 `ls 产物目录` 的输出贴进对话作为达成证据。
